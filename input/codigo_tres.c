


int main() {
    int i,x, y;
    // int a = 3 - 8, b, c, x;

    // x = (b + (b/2 - 4*a*c)) / (2*a);

    // while(i <= 3) {
    //     x = y;
    //     i = i +4;
    // }
    x /= 4;
    x += 3 - 4;
    

    // if(y == 0)
    // if(x >= 0)
    //     x = 0;
    // else{
    //     y = -x;
    // }
    // else {
    //     y = -x;
    // }

}