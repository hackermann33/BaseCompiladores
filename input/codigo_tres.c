


int main() {
    int x, y;
    // int a = 3 - 8, b, c, x;

    // x = (b + (b/2 - 4*a*c)) / (2*a);

    if(x >= 0)
        if(x >= 0)
            x = 0;
    else{
        y = -x;
    }
    // else {
    //     y = -x;
    // }

}