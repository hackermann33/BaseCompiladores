int f(int a, int b) {

    return 0;
}

int main() {
    int i,x, y;
    x = 4;
    // int a = 3 - 8, b, c, x;

    // x = (b + (b/2 - 4*a*c)) / (2*a);

    // while(i <= 3) {
    //     x = y;
    //     i = i +4;
    // }
    // for(int j = 0; i < x;){
    //     x+=3;
    // }

    // for(i=0; i < 10;i+=3)
    //     x+=3;

    int a = f(1+4+94,2-2) ;
    

    
    // if(y == 0)
    // if(x >= 0)
    //     x = 0;
    // else{
    //     y = -x;
    // }
    // else {
    //     y = -x;
    // }

}